<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basicfirebasesaervices2019">

    <application
        android:allowBackup="true"
        android:icon="@drawable/my_whatsapp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/my_whatsapp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MyWhatsAppActivity"
            android:label="@string/title_activity_my_whats_app"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".DatabaseActivity"
            android:label="@string/title_activity_data_base"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>